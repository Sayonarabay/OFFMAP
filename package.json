{
  "name": "OFFMAP",
  "version": "3.0.0",
  "description": "Travel destination finder with real prices via MCP",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0"
  },
  "engines": {
    "node": ">=18"
  }
}
